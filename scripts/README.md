# Reusable Scripts

This directory is reserved for reusable Bash examples that support the lessons.

The final project has its own workspace under `12-final-project/`.

Do not execute a script you do not understand, especially one containing destructive commands.
